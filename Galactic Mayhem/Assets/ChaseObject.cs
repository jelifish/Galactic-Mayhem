using UnityEngine;
using System.Collections;

public class ChaseObject : MonoBehaviour {
//	public GameObject target;
//	float speed = 5f;

	
	// Update is called once per frame
//	void FixedUpdate () {
//		this.gameObject.GetComponent<Rigidbody>().AddForce(target.transform.forward*-speed);
//	}
}
